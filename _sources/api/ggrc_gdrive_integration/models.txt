
.. WARNING! This file is autogenerated and should not be edited manually.

Models
======

``ObjectEvent``
---------------

..  class:: ggrc_gdrive_integration.models.object_event.ObjectEvent

    ..  warning::
        Missing doc-string of ``ggrc_gdrive_integration.models.object_event.ObjectEvent``

    Schema:

    ..  code-block:: sql

        CREATE TABLE object_events (
        	id INTEGER NOT NULL AUTO_INCREMENT, 
        	event_id VARCHAR(250) NOT NULL, 
        	calendar_id VARCHAR(250), 
        	eventable_id INTEGER NOT NULL, 
        	eventable_type VARCHAR(250) NOT NULL, 
        	updated_at DATETIME NOT NULL, 
        	modified_by_id INTEGER, 
        	created_at DATETIME NOT NULL, 
        	context_id INTEGER, 
        	PRIMARY KEY (id), 
        	FOREIGN KEY(context_id) REFERENCES contexts (id)
        )

    Mixins:

    *   :class:`ggrc.models.mixins.Base`
    *   :class:`ggrc.models.mixins.ChangeTracked`
    *   :class:`ggrc.models.mixins.ContextRBAC`
    *   :class:`ggrc.models.mixins.Identifiable`


``ObjectFile``
--------------

..  class:: ggrc_gdrive_integration.models.object_file.ObjectFile

    ..  warning::
        Missing doc-string of ``ggrc_gdrive_integration.models.object_file.ObjectFile``

    Schema:

    ..  code-block:: sql

        CREATE TABLE object_files (
        	id INTEGER NOT NULL AUTO_INCREMENT, 
        	file_id VARCHAR(250) NOT NULL, 
        	parent_folder_id VARCHAR(250), 
        	fileable_id INTEGER NOT NULL, 
        	fileable_type VARCHAR(250) NOT NULL, 
        	updated_at DATETIME NOT NULL, 
        	modified_by_id INTEGER, 
        	created_at DATETIME NOT NULL, 
        	context_id INTEGER, 
        	PRIMARY KEY (id), 
        	FOREIGN KEY(context_id) REFERENCES contexts (id)
        )

    Mixins:

    *   :class:`ggrc.models.mixins.Base`
    *   :class:`ggrc.models.mixins.ChangeTracked`
    *   :class:`ggrc.models.mixins.ContextRBAC`
    *   :class:`ggrc.models.mixins.Identifiable`


``ObjectFolder``
----------------

..  class:: ggrc_gdrive_integration.models.object_folder.ObjectFolder

    ..  warning::
        Missing doc-string of ``ggrc_gdrive_integration.models.object_folder.ObjectFolder``

    Schema:

    ..  code-block:: sql

        CREATE TABLE object_folders (
        	id INTEGER NOT NULL AUTO_INCREMENT, 
        	folder_id VARCHAR(250) NOT NULL, 
        	parent_folder_id VARCHAR(250), 
        	folderable_id INTEGER NOT NULL, 
        	folderable_type VARCHAR(250) NOT NULL, 
        	updated_at DATETIME NOT NULL, 
        	modified_by_id INTEGER, 
        	created_at DATETIME NOT NULL, 
        	context_id INTEGER, 
        	PRIMARY KEY (id), 
        	FOREIGN KEY(context_id) REFERENCES contexts (id)
        )

    Mixins:

    *   :class:`ggrc.models.mixins.Base`
    *   :class:`ggrc.models.mixins.ChangeTracked`
    *   :class:`ggrc.models.mixins.ContextRBAC`
    *   :class:`ggrc.models.mixins.Identifiable`


